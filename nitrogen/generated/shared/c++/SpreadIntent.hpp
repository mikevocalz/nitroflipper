///
/// SpreadIntent.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitroflipper {

  /**
   * An enum which can be represented as a JavaScript union (SpreadIntent).
   */
  enum class SpreadIntent {
    NONE      SWIFT_NAME(none) = 0,
    LANDSCAPE      SWIFT_NAME(landscape) = 1,
    PORTRAIT      SWIFT_NAME(portrait) = 2,
    BOTH      SWIFT_NAME(both) = 3,
    AUTO      SWIFT_NAME(auto) = 4,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitroflipper

namespace margelo::nitro {

  // C++ SpreadIntent <> JS SpreadIntent (union)
  template <>
  struct JSIConverter<margelo::nitro::nitroflipper::SpreadIntent> final {
    static inline margelo::nitro::nitroflipper::SpreadIntent fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("none"): return margelo::nitro::nitroflipper::SpreadIntent::NONE;
        case hashString("landscape"): return margelo::nitro::nitroflipper::SpreadIntent::LANDSCAPE;
        case hashString("portrait"): return margelo::nitro::nitroflipper::SpreadIntent::PORTRAIT;
        case hashString("both"): return margelo::nitro::nitroflipper::SpreadIntent::BOTH;
        case hashString("auto"): return margelo::nitro::nitroflipper::SpreadIntent::AUTO;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum SpreadIntent - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::nitroflipper::SpreadIntent arg) {
      switch (arg) {
        case margelo::nitro::nitroflipper::SpreadIntent::NONE: return JSIConverter<std::string>::toJSI(runtime, "none");
        case margelo::nitro::nitroflipper::SpreadIntent::LANDSCAPE: return JSIConverter<std::string>::toJSI(runtime, "landscape");
        case margelo::nitro::nitroflipper::SpreadIntent::PORTRAIT: return JSIConverter<std::string>::toJSI(runtime, "portrait");
        case margelo::nitro::nitroflipper::SpreadIntent::BOTH: return JSIConverter<std::string>::toJSI(runtime, "both");
        case margelo::nitro::nitroflipper::SpreadIntent::AUTO: return JSIConverter<std::string>::toJSI(runtime, "auto");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert SpreadIntent to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("none"):
        case hashString("landscape"):
        case hashString("portrait"):
        case hashString("both"):
        case hashString("auto"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
