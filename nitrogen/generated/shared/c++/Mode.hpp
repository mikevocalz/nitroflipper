///
/// Mode.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitroflipper {

  /**
   * An enum which can be represented as a JavaScript union (Mode).
   */
  enum class Mode {
    SINGLE      SWIFT_NAME(single) = 0,
    SPREAD      SWIFT_NAME(spread) = 1,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitroflipper

namespace margelo::nitro {

  // C++ Mode <> JS Mode (union)
  template <>
  struct JSIConverter<margelo::nitro::nitroflipper::Mode> final {
    static inline margelo::nitro::nitroflipper::Mode fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("single"): return margelo::nitro::nitroflipper::Mode::SINGLE;
        case hashString("spread"): return margelo::nitro::nitroflipper::Mode::SPREAD;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum Mode - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::nitroflipper::Mode arg) {
      switch (arg) {
        case margelo::nitro::nitroflipper::Mode::SINGLE: return JSIConverter<std::string>::toJSI(runtime, "single");
        case margelo::nitro::nitroflipper::Mode::SPREAD: return JSIConverter<std::string>::toJSI(runtime, "spread");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert Mode to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("single"):
        case hashString("spread"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
