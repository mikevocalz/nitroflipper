///
/// Leaf.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitroflipper {

  /**
   * An enum which can be represented as a JavaScript union (Leaf).
   */
  enum class Leaf {
    RECTO      SWIFT_NAME(recto) = 0,
    VERSO      SWIFT_NAME(verso) = 1,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitroflipper

namespace margelo::nitro {

  // C++ Leaf <> JS Leaf (union)
  template <>
  struct JSIConverter<margelo::nitro::nitroflipper::Leaf> final {
    static inline margelo::nitro::nitroflipper::Leaf fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("recto"): return margelo::nitro::nitroflipper::Leaf::RECTO;
        case hashString("verso"): return margelo::nitro::nitroflipper::Leaf::VERSO;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum Leaf - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::nitroflipper::Leaf arg) {
      switch (arg) {
        case margelo::nitro::nitroflipper::Leaf::RECTO: return JSIConverter<std::string>::toJSI(runtime, "recto");
        case margelo::nitro::nitroflipper::Leaf::VERSO: return JSIConverter<std::string>::toJSI(runtime, "verso");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert Leaf to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("recto"):
        case hashString("verso"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
