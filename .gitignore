node_modules
build
lib
ios/build
android/build
*.log
.DS_Store
.vscode
.idea
.gradle
.cxx
*.cbz
build-mupdf/

# MuPDF Apple build output. Tens of megabytes, rebuilt by
# scripts/build-mupdf-apple.sh at pod install time.
ios/generated/
build/

# Device E2E artifacts (screenshots, reports)
e2e-artifacts/

# Native build trees. A MuPDF build drops a 175MB static library in here, over
# GitHub's hard file-size limit, and the whole tree is regenerable from
# scripts/fetch-mupdf.sh plus a cmake run.
build-android/
build-ios-sim/
build-ios-device/

# Python bytecode from the device scripts
__pycache__/
